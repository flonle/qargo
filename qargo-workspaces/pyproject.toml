[project]
name = "qargo-workspaces"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["click"]

[project.scripts]
qws = "qargo_workspaces.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
